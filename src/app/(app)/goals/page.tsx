import { PagePlaceholder } from "@/components/page-placeholder";

export default function GoalsPage() {
  return (
    <PagePlaceholder
      title="Goals"
      description="Track leads that reached their goal through to a closed sale. Built in Phase 6."
    />
  );
}
