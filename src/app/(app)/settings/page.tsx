import { PagePlaceholder } from "@/components/page-placeholder";

export default function SettingsPage() {
  return (
    <PagePlaceholder
      title="Settings"
      description="Profile, users, integrations, and workspace configuration. Built out across later phases."
    />
  );
}
