import { PagePlaceholder } from "@/components/page-placeholder";

export default function CostsPage() {
  return (
    <PagePlaceholder
      title="Costs"
      description="Per-call and per-campaign spend with budget caps. Built in Phase 7."
    />
  );
}
