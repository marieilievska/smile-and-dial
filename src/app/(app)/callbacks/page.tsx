import { PagePlaceholder } from "@/components/page-placeholder";

export default function CallbacksPage() {
  return (
    <PagePlaceholder
      title="Callbacks"
      description="Scheduled callbacks and their automatic dialing. Built in Phase 6."
    />
  );
}
